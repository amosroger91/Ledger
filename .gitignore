node_modules/
dist/
dist-ssr/
*.local
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# Rust / WASM build artifacts — main stays pure source; the WASM package is
# (re)built by scripts/build-wasm.mjs locally and in CI, never committed.
wasm/target/
wasm/pkg/
